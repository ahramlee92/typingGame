<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.firstProject.typinggame">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MyPageActivity" />
        <activity android:name=".ScoreActivity" />
        <activity android:name=".GroupGameActivity" />
        <activity android:name=".GameWaitingActivity" />
        <activity android:name=".MakingRoomActivity" />
        <activity android:name=".GroupWatingActivity" />
        <activity android:name=".StartActivity" />
        <activity android:name=".SingleActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>